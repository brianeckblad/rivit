# Application Requirements
# Install on production server: pip install -r requirements.txt
#
# Versions are pinned to known-good releases for reproducible deployments.
# To upgrade, bump the version below, run `pip install -r requirements.txt`,
# verify the app boots, and re-run `pip-audit` (or this repo's CVE check).

# Flask web framework
Flask==3.1.3

# Werkzeug (Flask dependency)
Werkzeug==3.1.6

# Production WSGI server
gunicorn==25.1.0

# AWS SDK for Python (S3 uploads, Secrets Manager, CloudWatch, SNS)
boto3==1.42.65
botocore[crt]==1.42.65

# Image processing library for thumbnail generation
# 12.2.0 fixes CVE-2026-40192 (FITS GZIP decompression bomb)
Pillow==12.2.0

# HTTP library for eBay API integration
# 2.33.0 fixes CVE-2026-25645 (extract_zipped_paths temp-file reuse)
requests==2.33.0
ebaysdk==2.2.0

# Environment variable management (for local dev, production uses Secrets Manager)
python-dotenv==1.2.2
