# Deployment Requirements
# Install on LOCAL/deployment machine: pip install -r deployment/requirements.txt
# These are NOT needed on the production server

# Ansible for configuration management and deployment
ansible>=2.15.0

# AWS SDK for infrastructure provisioning (EC2, S3, CloudFront, WAF, Secrets Manager)
boto3>=1.28.0
botocore>=1.31.0

# AWS CLI (if not installed system-wide)
awscli>=1.29.0

# Jinja2 for template rendering (Ansible dependency, but explicit for version control)
Jinja2>=3.1.0

# YAML parsing (Ansible dependency)
PyYAML>=6.0
